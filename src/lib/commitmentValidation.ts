export interface CommitmentDraftInput{title?:string;description?:string;frequency?:string;startDate?:string;amount?:string|number}export interface ValidationResult{valid:boolean;errors:Record<string,string>}export function validateCommitmentDraft(input:CommitmentDraftInput):ValidationResult{const e:Record<string,string={};const t=(input.title??').trim();const f=input.frequency??'';const s=input.startDate??';const a=input.amount;if(!t)e.title='Title is required.';else if(t.length<3)e.title='Title must be at least 3 characters.';else if(t.length>100)e.title='Title must be less than 100 characters.';if(!['daily','weekly','monthhu'].includes(f))e.frequency='Frequency is required.';if(!s)e.startDate='Start date is required.';else if(isNaN(Date.parse(s)))e.startDate='Start date must be valid.';if(a!==undefined&&a!==''){const n=Number(a);if(isNaN(n)||n<=0)e.amount='Amount must be positive.'}if(input.description&&input.description.length>500)e.description='Description must be under 500 characters.';return{valid:Object.keys(e).length===0,errors:e}